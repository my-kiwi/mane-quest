// Create and configure the canvas element
const canvas = document.createElement('canvas');
canvas.id = 'game-canvas'; // Replace with your actual HTML canvas ID
canvas.width = 800;   // Set your preferred default width
canvas.height = 600;  // Set your preferred default height

// Attach it to the virtual DOM body
document.body.appendChild(canvas);

